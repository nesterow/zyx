import Separated from "./Separated.tsx";
import { Meta } from "@storybook/addon-docs/blocks";

export const Separator = () => (
  <span className="inline-block bg-green-500 h-[8px] w-[8px] mx-4 rounded-full"></span>
);

export const itemClass =
  "bg-white py-2 px-4 rounded-lg text-sm font-mono focus:ring-2 focus:ring-green-400 border-1 border-gray-300 border-dashed";

<Meta title="Separated Anatomy" />

<Separated
  separator={<Separator />}
  className="flex items-center gap-4 bg-white py-2 px-3 rounded-md font-sans border-1 border-gray-300 border-dashed"
>
  <span className={itemClass}>Start</span>
  {Array.from({ length: 5 }, (_, i) => (
    <button className={itemClass} key={i}>
      item {i + 1}
    </button>
  ))}
  <span className={itemClass}>End</span>
</Separated>

```tsx
<Separated separator={<Separator />}>
  <span>Start</span>
  {Array.from({ length: 5 }, (_, i) => (
    <button key={i}>item {i + 1}</button>
  ))}
  <span>End</span>
</Separated>
```
